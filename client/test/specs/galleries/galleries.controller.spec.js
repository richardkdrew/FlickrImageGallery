'use strict';

describe('controller: galleries', function () {
  /*var controller;

  beforeEach(function () {
    module('app.galleries');
    inject(function($injector) {
      controller = $injector.get('Galleries');
    })
  });

  it('should be defined', function ()
  {
    expect(controller).toBeDefined();
  });*/

});
